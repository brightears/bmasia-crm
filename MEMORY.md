# Lessons / Gotchas

1. **DRF serializer whitelist = silent write-drop.** A ModelSerializer with an explicit `Meta.fields` list silently DROPS any input field not in the list on update_record/PATCH — returns success, value unchanged, NO validation error. To make a model field settable via the API or MCP `update_record`, add it to the serializer's `Meta.fields` (and keep it out of `read_only_fields`). Hit twice: `lifecycle_type` (2026-05-21) and `first/second_followup_sent` (2026-06-09, commit 63fea98). `update_record` in crm_app/mcp.py writes through the serializer mapped in `_COLLECTION_MAP`, NOT the MCP `ModelQueryToolset.fields` list.

2. **Contract status has NO code-enforced state machine.** `Contract.status` is a plain Django choices CharField — any valid value is accepted from any prior value (no transition validation in serializer/model/view). The Draft→Sent→Active→… lifecycle is enforced by (a) the mcp.py ContractQuery doc-string hint and (b) Cira's charter judgment (cira-agent/CLAUDE.md), NOT by code. Revenue recognition is fully DECOUPLED from contract status (driven by Invoices + RevenueRecognitionSchedule's own status field), so e.g. a Sent→Cancelled never-Active contract touches zero revenue code. LATENT BUG (spun off separately): `handle_contract_termination` signal (crm_app/signals.py ~line 42) and `_determine_lifecycle_type` churn (crm_app/services/revenue_tracking_service.py ~line 98) both gate on `status=='Terminated'`, which is NOT a valid Contract status → both are dead code (zone-cleanup-on-termination + churn auto-classification never fire).
